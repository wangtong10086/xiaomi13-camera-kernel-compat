# 贡献指南

[English](CONTRIBUTING.md)

欢迎提交可复现、可审阅且明确针对 Xiaomi 13（`fuxi`）的贡献。

1. 从 `UPSTREAMS.md` 固定的上游提交开始，并保持 checkout 干净。
2. 按子系统拆分改动，说明故障现象、目标 ROM/内核及回滚结果。
3. 不得提交固件、完整厂商源码树、设备转储、签名密钥、序列号、token、含个人数据的日志、专有 APK 或 ROM 镜像。
4. 保留已有版权与许可证声明；不要为厂商派生文件猜测 SPDX 标识。
5. PR 前在全新的固定提交 checkout 上运行 `scripts/Apply-Overlay.ps1`，并完成 shell 语法与静态检查。
6. 不得加入一键刷写命令。文档必须要求明确 serial、精确分区、已校验哈希和可用回滚镜像。

兼容性报告请提供不敏感的构建指纹、精确提交、哈希、脱敏日志以及回滚是否成功。
